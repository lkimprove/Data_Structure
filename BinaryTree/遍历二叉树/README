1.层序遍历
  一种非递归的遍历二叉树方法，利用队列的原理来实现层序遍历。
    1）若根结点非空，将根结点进行入队操作；
    2）若队中不为空，则重复以下操作
      a.取出队首元素，打印并删除
      b.将左右子树不为空，则将其入队
  代码会有两种的实现
    1）在C中创建一个队列，完成上述操作
    2）直接利用C++中容器类的模板的头文件<queue>，完成上述操作
   
