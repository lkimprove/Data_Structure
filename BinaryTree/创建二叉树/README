根据三种递归遍历出来的序列创建二叉树。
前序和后序序列都便于查找根结点（preorder[0] 与 postorder[0]）， 中序序列便于查找左子树。
一般通过前序或后序序列取出根结点，然后通过根结点在中序中的位置来确定左子树的长度，进而通过左子树的长度可以确定右子树的长度，从而构建二叉树。
创建方式为：1）前序 + 中序   2）中序 + 后序
注意：
  1）前序 + 后序 只能找到根结点的位置，无法确定左右子树的长度，所以无法创建二叉树。
  2）在代码实现中，需要通过根结点确定左右子树的长度，所以需要保证序列中不存在相同的结点。
